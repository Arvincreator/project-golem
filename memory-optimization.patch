diff --git a/src/managers/AutonomyManager.js b/src/managers/AutonomyManager.js
index 3101b32..abc1234 100644
--- a/src/managers/AutonomyManager.js
+++ b/src/managers/AutonomyManager.js
@@ -18,6 +18,7 @@ class AutonomyManager {
         this.tgBot = null;
         this.dcClient = null;
         this.convoManager = null;
+        this._timeWatcherTimer = null;
         this.pendingPatch = null;
     }
 
@@ -33,8 +34,20 @@ class AutonomyManager {
         }
         console.log(`🚀 [Autonomy][${this.golemId}] Starting autonomy services...`);
         this.resumeOrScheduleAwakening();
-        setInterval(() => this.timeWatcher(), 60000);
+        if (this._timeWatcherTimer) clearInterval(this._timeWatcherTimer);
+        this._timeWatcherTimer = setInterval(() => this.timeWatcher(), 60000);
+        if (typeof this._timeWatcherTimer.unref === 'function') this._timeWatcherTimer.unref();
         this.archiveTimer = null;
         this.scheduleNextArchive();
+    }
+
+    stop() {
+        if (this._timeWatcherTimer) {
+            clearInterval(this._timeWatcherTimer);
+            this._timeWatcherTimer = null;
+        }
+        if (this.archiveTimer) clearTimeout(this.archiveTimer);
+        if (this.awakeTimer) clearTimeout(this.awakeTimer);
     }
 
diff --git a/apps/dashboard/plugin.js b/apps/dashboard/plugin.js
index 4d83262..def4567 100644
--- a/apps/dashboard/plugin.js
+++ b/apps/dashboard/plugin.js
@@ -160,6 +160,10 @@ class DashboardPlugin {
     detach() {
         this.manager.state.isDetached = true;
         ConsoleInterceptor.restore();
+        if (this.timer) {
+            clearInterval(this.timer);
+            this.timer = null;
+        }
         if (this.view) this.view.destroy();
 
         if (this.webServer) {

diff --git a/web-dashboard/src/components/LogStream.tsx b/web-dashboard/src/components/LogStream.tsx
index f0a5d44..1234567 100644
--- a/web-dashboard/src/components/LogStream.tsx
+++ b/web-dashboard/src/components/LogStream.tsx
@@ -49,7 +49,7 @@ export function LogStream({
 
         const handleLog = (payload: unknown) => {
             if (!isLogMessage(payload)) return;
-            setLogs((prev) => [...prev.slice(-199), payload]); // Keep last 200 logs
+            setLogs((prev) => [...prev.slice(-149), payload]); // Keep last 150 logs
         };
 
         socket.on("init", handleInit);

diff --git a/web-dashboard/src/components/AgentChat.tsx b/web-dashboard/src/components/AgentChat.tsx
index 3b91927..abcdef0 100644
--- a/web-dashboard/src/components/AgentChat.tsx
+++ b/web-dashboard/src/components/AgentChat.tsx
@@ -93,7 +93,7 @@ export function AgentChat() {
                     isSystem = sender === "MultiAgent" || sender === "InteractiveMultiAgent";
                 }
 
-                setMessages((prev) => [...prev.slice(-1000), {
+                setMessages((prev) => [...prev.slice(-499), {
                     id: Date.now().toString() + Math.random(),
                     sender,
                     content,
